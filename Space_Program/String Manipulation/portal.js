function portal();
{
    // accessing the element from the html
    document.getElementById("clickHere");
    // calling an alert on the page
    alert("Open Sesame!")
    // replacing the index page with the requested page.
    location.replace("UAT_Space_Page3.html")
}